class Solution:
    def findRotation(self, mat, target):
        res1 = [[0 for i in range(len(mat))] for j in range(len(mat[0]))]
        rot = 0
        for i in range(len(mat)):
            for i in range(len(mat[0])):
                pass

